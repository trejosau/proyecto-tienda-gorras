import express from 'express';
// import { createDelivery, getDelivery, updateDeliveryLocation, deleteDelivery } from '../controllers/deliveryController';

const router = express.Router();
/*
// Crear una nueva entrega
router.post('/deliveries', createDelivery);

// Obtener una entrega por ID
router.get('/deliveries/:id', getDelivery);

// Actualizar la ubicación de una entrega
router.put('/deliveries/:id/location', updateDeliveryLocation);

// Eliminar una entrega por ID
router.delete('/deliveries/:id', deleteDelivery);
*/
export default router;